from setuptools import setup, find_packages

setup(
    name="index_engine",
    version="0.1.0",
    description="A modular, extensible framework for building and querying data indexes.",
    author="pari9p",
    author_email="behlpari4@gmail.com",
    packages=find_packages(),
    install_requires=[],
    python_requires=">=3.8",
)
